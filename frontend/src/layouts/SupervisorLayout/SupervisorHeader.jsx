import React from 'react';

export const SupervisorHeader = () => {
  return (
    <header style={{ padding: '16px', background: '#fff', borderBottom: '1px solid var(--border-default)' }}>
      <h3>Supervisor App (Mobile)</h3>
    </header>
  );
};

export default SupervisorHeader;

