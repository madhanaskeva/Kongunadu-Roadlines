import React from 'react';

export const SupervisorDashboard = () => (
  <div style={{ padding: '24px', backgroundColor: '#ffffff', borderRadius: 'var(--radius-lg)' }}>
    <h3>Supervisor Mobile App Dashboard (Separate App)</h3>
    <p>Supervisor functionality is built separately in the dedicated mobile application.</p>
  </div>
);

export default SupervisorDashboard;

